<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
	<domain-config cleartextTrafficPermitted="true">
    <!-- For JustAudio Streaming (uses local http proxy) -->
		<domain includeSubdomains="false">127.0.0.1</domain> 
    <!-- For local model download during dev/test -->
		<domain includeSubdomains="false">10.59.179.170</domain> 
	</domain-config>
</network-security-config>