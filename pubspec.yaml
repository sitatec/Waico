name: waico
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_ai_toolkit: ^0.9.1
  flutter_gemma: ^0.9.0
  background_downloader: ^9.2.2
  health: ^13.1.1
  permission_handler: ^11.3.1
  stts: ^1.2.4
  cross_file: ^0.3.4+2
  kokoro_tts_flutter: ^0.1.1
  just_audio: ^0.10.4
  audio_session: ^0.2.2
  siri_wave: ^2.3.0
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
